{
  "name": "edge_marker",
  "version": "0.0.1",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc -b && cp markers.yaml dist/markers.yaml",
    "test": "vitest run --coverage",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "yaml": "^2.8.0"
  }
}
